package routes

import (
	"github.com/gin-gonic/gin"
)

func InitGoalRoutes(router *gin.RouterGroup) {
	//handler := handlers.NewGoalHandler(db)
	//
	//router.GET("/", handler.Index)
	//router.POST("/", handler.Store)
}
